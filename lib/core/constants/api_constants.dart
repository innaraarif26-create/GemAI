/// Central location for API endpoints used across the app.
class ApiConstants {
  ApiConstants._();

  /// Google Books API base URL for volume search.
  static const String googleBooksBaseUrl =
      'https://www.googleapis.com/books/v1/volumes';
}
